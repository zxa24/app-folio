Strict offline mode expects these files in this folder:

- required: `pdf.min.js`
- optional for `file://`: `pdf.worker.min.js`
  - required only for `http://` / `https://` serving

No CDN fallback is used.
